% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{set.activeIdent}
\alias{set.activeIdent}
\title{Set the active ident or annotation labels for an scTypeEval object.}
\usage{
set.activeIdent(scTypeEval, ident = NULL)
}
\arguments{
\item{scTypeEval}{An \code{scTypeEval} object.}

\item{ident}{A character string specifying the column in \code{scTypeEval@metadata} to set as the active identity.}
}
\value{
The modified \code{scTypeEval} object with the active identity set.
}
\description{
This function assigns a specific cell type annotation from the metadata as the active identity
in an \code{scTypeEval} object, allowing for downstream analysis based on the selected classification.
}
\details{
The function ensures that the provided identity exists within the metadata before setting it.
If no valid identity is provided, an error is raised.
}
\examples{
\donttest{
# Create synthetic test data
library(Matrix)
counts <- Matrix(rpois(1000, 5), nrow = 50, ncol = 20, sparse = TRUE)
rownames(counts) <- paste0("Gene", 1:50)
colnames(counts) <- paste0("Cell", 1:20)

metadata <- data.frame(
  celltype = rep(c("TypeA", "TypeB"), each = 10),
  sample = rep(paste0("Sample", 1:4), each = 5),
  row.names = colnames(counts)
)

# Create scTypeEval object from matrix
sceval <- create.scTypeEval(matrix = counts, metadata = metadata)

# add active ident
sceval <- set.activeIdent(sceval, ident = "celltype")
}

}
